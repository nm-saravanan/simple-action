name: Action flow

on: [push]

jobs:
  run-action:
    runs-on: ubuntu-latest
    steps:
      - name: List files
        run: :|
          pwd
          ls -a
      - name: check out
        uses: actions/checkout
      - name: simple js action
        id: greet
        uses: saubermacherag/ansible-playbook-docker-action@v1.3
        with:
            playbookName: 'play1.yml'
      - name: Log the time
        run: echo "{{steps.greet.outputs.time}}"